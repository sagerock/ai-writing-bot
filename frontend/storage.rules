rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Uploads and signed downloads are mediated by the authenticated backend.
    // The Firebase Admin SDK bypasses these browser-facing rules.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
